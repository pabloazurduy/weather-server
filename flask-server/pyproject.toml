[project]
name = "weather-server"
version = "0.1.0"
description = "Weather dashboard server for e-ink devices"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.2.0",
    "requests>=2.33.1",
]
